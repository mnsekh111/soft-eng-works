var a = require("a");
var b = require("b");

var str1 = "str1"

function as(param1,param2)
{
    var str2 = "str2";
    if(a==b && b <c)
    {
        if(a<d || b <c || c === "str3")
        {

        }

        a.b(s).c[i]
    }
    while(a==a)
    {

    }

    mints.name.toString().split(".")[0];
    mints.name.toString().split(".")[0].split(",");

    return 0
}


function ass()
{
    mints.name.toString().split(".")[0];
    mints.name.toString().split(".")[0].split(",").split()[0].toString();
    while(a>c && b==d)
    {
        if(a==c && d<f)
        {
            if(a==f)
            {
            }
        }
    }
}